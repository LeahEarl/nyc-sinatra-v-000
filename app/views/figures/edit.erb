<h1>Edit <%= @figure.name %></h1>

<form action="/figures/<%= @figure.id %>" method="post">
  <input type="hidden" name="_method" value="patch">

  <h3><label>The Figure's name:</label>
  <input type="text" name="figure[name]" id="figure_name" value="<%= @figure.name %>"></h3>

  <h3>and title(s):
  <% @titles.each do |title| %>
  <input type="checkbox" name="figure[title_ids][]" value="<%=title.id%>" id="title_<%=title.id%>" <%='checked' if @figure.titles.include?(title)%>><%= title.name %>
  <% end %></h3>

  <h3>or create a title:
  <input type="text" name="title[name]" id="new_title"></h3>

  <h3>and select any associated landmarks:
  <ul>
    <% @landmarks.each do |landmark| %>
    <li><input type="checkbox" name="figure[landmark_ids][]" value="<%=landmark.id%>" id="landmark_<%=landmark.id%>" <%='checked' if @figure.landmarks.include?(landmark)%>><%= landmark.name %>
    <% end %></li>
  </ul>

  <h3>or create a landmark:
  <input type="text" name="landmark[name]" id="new_landmark"></h3>

  <input type="submit" id="submit" value="Edit Figure">
</form>
