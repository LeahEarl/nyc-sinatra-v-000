<h1>Create a New Figure</h1>

<form action="/figures" method="post">
  <h3><label>The Figure's name:</label>
  <input type="text" name="figure[name]" id="figure_name"></h3>

  <h3>and title(s):
  <% @titles.each do |title| %>
  <input type="checkbox" name="figure[title_ids][]" value="<%=title.id%>" id="title_<%=title.id%>"><%= title.name %>
  <% end %></h3>

  <h3>or create a title:
  <input type="text" name="title[name]" id="new_title"></h3>

  <h3>and select any associated landmarks:
  <ul>
    <% @landmarks.each do |landmark| %>
    <li><input type="checkbox" name="figure[landmark_ids][]" value="<%=landmark.id%>" id="landmark_<%=landmark.id%>"><%= landmark.name %>
    <% end %></li>
  </ul>

  <h3>or create a landmark:
  <input type="text" name="landmark[name]" id="new_landmark"></h3>

  <input type="submit" id="submit" value="Create New Figure">
</form>
